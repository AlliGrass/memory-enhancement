export const MemoryCards = () => {
    return (
        <section>
            {/* difficulty easy: all visible
                difficulty hard: all hidden, match translations */}
        </section>
    )
}

